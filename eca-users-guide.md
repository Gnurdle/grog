# ECA Users' Guide — the grog MCP servers

Quick setup guide for enabling the **grog MCP servers** inside ECA (the VS Code
agent) so its model can use your Odoo, imaging, office and memory tools.

> This guide shows the **sudo-style "where does each file go"** so a teammate can
> get the Odoo tools working in their own editor without touching this project's
> secrets.

---

## What you get

| MCP server | Tools | Notes |
|---|---|---|
| `grog-odoo` | `odoo_list_instances`, `odoo_use_instance`, `odoo_authenticate`, `odoo_search_read`, `odoo_get_fields`, `odoo_execute_sql` | **Strictly read-only** — cannot create/update/delete anything; SQL limited to `SELECT/WITH/SHOW/EXPLAIN/DESCRIBE/VALUES/TABLE`. |
| `grog-imaging` | `read_pdf_document`, `ocr_pdf_document`, `analyze_pdf_line_drawings`, `read_office_document`, … | PDF/OCR/imaging (needs Tesseract). |
| `grog-office` | `import_document`, `list_blocks`, `get_text`, `find_text`, `replace_text`, `render`, `save`, … | docx manipulation + rendering (needs LibreOffice). |
| `grog-memory` | `assoc_store`, `assoc_get`, `assoc_keys`, `assoc_delete`, `assoc_search` | Simple key/value memory. |

*(Usually you only care about `grog-odoo` — the others are optional.)*

---

## 1. Prereqs

- **ECA** installed + the VS Code extension.
- The repo checkout containing `grog-odoo/` (grab just that directory if you like).
- Your runtime choice:
  - **Source mode:** needs the **Clojure CLI** (`clojure` on PATH).
  - **Uberjar mode:** needs only a **JRE 17+** — best for teammates who aren't Clojure devs.
- **Odoo credentials** (your own, not ours).

---

## 2. Where ECA reads its config

Add a top-level `"mcpServers"` block to ECA's config file:

- **Linux / macOS:** `~/.config/eca/config.json`
- **Windows:** `%USERPROFILE%\.config\eca\config.json`

The keys you use in ECA config: `command`, `args`, `env` (plus any normal ECA settings already there).

---

## 3. Files that go where

| File | Where | Purpose |
|---|---|---|
| `grog-odoo/` source | anywhere, e.g. `~/tools/grog-odoo` | the server (source mode runs from this dir) |
| `grog-odoo/target/grog-odoo.jar` | same folder | the bundled jar (`./build-uberjar.sh`) — no Clojure CLI needed at runtime |
| `odoo-instances.json` | e.g. `~/.config/odoo-instances.json` (gitignore it) | Odoo connection(s) — *your* credentials live here |
| `~/.config/grog/...` | generated by grog | only relevant if you also run grog itself |

---

## 4. Config snippets

### Odoo — multi-instance (via config file)

First create `~/.config/odoo-instances.json`:

```json
{
  "instances": [
    { "name": "stage", "url": "https://your-odoo.example.com",
      "db": "odoo18_stage", "user": "admin", "password": "CHANGE_ME" }
  ]
}
```
*Optional per-instance raw-SQL connection (needed for `odoo_execute_sql`):*
```json
"sql": { "type": "postgres", "host": "127.0.0.1", "port": 5432,
         "db": "odoo18_stage", "user": "odoo", "password": "CHANGE_ME" }
```

Then in `config.json`:

**Using the jar (recommended for teammates):**
```json
"mcpServers": {
  "grog-odoo": {
    "command": "java",
    "args": ["-cp", "C:/path/to/grog-odoo/target/grog-odoo.jar",
             "clojure.main", "-m", "grog-odoo.main"],
    "env": { "GROG_ODOO_CONFIG": "C:/Users/you/.config/odoo-instances.json" }
  }
}
```

**Or from source (needs Clojure CLI):**
```json
"mcpServers": {
  "grog-odoo": {
    "command": "bash",
    "args": ["-lc", "cd '~/tools/grog-odoo' && clojure -M:mcp -m grog-odoo.main"],
    "env": { "GROG_ODOO_CONFIG": "~/.config/odoo-instances.json" }
  }
}
```

### Odoo — single instance (simplest; no config file)

```json
"mcpServers": {
  "grog-odoo": {
    "command": "java",
    "args": ["-cp", "C:/path/to/grog-odoo/target/grog-odoo.jar",
             "clojure.main", "-m", "grog-odoo.main"],
    "env": {
      "GROG_ODOO_URL": "https://your-odoo.example.com",
      "GROG_ODOO_DB": "odoo18",
      "GROG_ODOO_USER": "admin",
      "GROG_ODOO_PASSWORD": "CHANGE_ME"
    }
  }
}
```

### Also enabling imaging / office / memory (same repo)

```json
"mcpServers": {
  "grog-odoo":    { /* as above */ },
  "grog-imaging": {
    "command": "bash",
    "args": ["-lc", "cd '<REPO>/grog-imaging' && clojure -M:mcp -m grog-imaging.main"]
  },
  "grog-office": {
    "command": "bash",
    "args": ["-lc", "cd '<REPO>/grog-office' && clojure -M:mcp -m grog-office.main"]
  },
  "grog-memory": {
    "command": "bash",
    "args": ["-lc", "cd '<REPO>/grog-memory' && PYTHONPATH=src .venv/bin/python -m grog_memory.server"],
    "env": { "GROG_MEMORY_DB": "<REPO>/.grog-memory.db" }
  }
}
```

> `grog-imaging` needs Tesseract on PATH; `grog-office` needs LibreOffice. Skip them if those aren't installed.

---

## 5. Enable it

After editing `config.json`, **reload the window / restart ECA** in VS Code
(`ECA: Restart` or the reload-window command). ECA spawns the servers on startup
and the agent immediately sees the tools as `grog-odoo__…`, etc.

---

## 6. Try it

- "Use the stage instance" → `grog-odoo__odoo_use_instance("stage")`
- "Find the last 5 open sales orders" →
  `grog-odoo__odoo_search_read("sale.order", [["state","=","sale"]], ["name","amount_total","partner_id"], 5)`
- "Which products are inactive?" →
  `grog-odoo__odoo_execute_sql("SELECT name, active FROM product_product WHERE active = false")`

---

## 7. Approval & security

- ECA will **ask before running** these tools until you toggle **Trust/YOLO** mode.
- `grog-odoo` is **read-only by design** — no write tools exist, and SQL is
  restricted to read-only statements.
- **Never commit credentials.** Use the env-var form or a `gitignored`
  `odoo-instances.json`; don't paste the password into a shared config.
- Each teammate configures **their own** instances/credentials.

---

## 8. Troubleshooting

- **Server never appears after reload** → wrong command/args or deps didn't
  download. In source mode, run once manually:
  ```bash
  cd <REPO>/grog-odoo && clojure -M:mcp -m grog-odoo.main
  ```
  In jar mode, verify it starts:
  ```bash
  java -cp <REPO>/grog-odoo/target/grog-odoo.jar clojure.main -m grog-odoo.main
  ```
  (It should print nothing and idle on stdio.)
- **"Missing env GROG_ODOO_URL"** or **"…no :sql backend configured"** → check the
  `env` block / `odoo-instances.json` terms.
- **Needs Java 17+** for the jar path; check `java -version`.
